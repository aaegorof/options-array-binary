import { createAction } from "redux-act";

export const setup = createAction("setup");
export const toggleOption = createAction("toggleOption");
export const updateVal = createAction("updateVal");
export const addOption = createAction("addOption");
